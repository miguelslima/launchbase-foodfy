{% extends "admin/layout.njk" %}

{% block content %}
  <section>
    <div class="banner-index">
      <p class="title">As melhores receitas</p>
      <p class="text">Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
      </p>
      <div class="chef-img">
        <img src="/assets/chef.png" alt="chef">
      </div>
    </div>
  </section>

  <section class="cards">
    {% for recipe in recipes %}
    <div class="card" id="{{loop.index -1}}">
      <div class="card__image">
        <img src="{{recipe.image}}">
      </div>
      <div class="card-title">
        <p>{{recipe.title}}</p>
      </div>
      <div class="card-author">
        <p>{{recipe.author}}</p>
      </div>
    </div>
    {% endfor %}
  </section>

  
{%endblock%}
  
