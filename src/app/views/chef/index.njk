{% extends "chef/layout.njk" %}

{% block content %}


  <section id="admin-chef">
    <div class="form-chefs">
      <h1>Gerenciar chefs</h1>
      <span>
        <a href="chefs/create" class="btn">Adicionar chef</a>
      </span>
    </div>

    <div class="cards-chef">
      {% for chef in chefs %}
        <div class="cards-chef">
          <div class="chef-image">
            <img src="{{chef.avatar.url}}" alt="Foto do Chef">
          </div>

          <div class="card-info-chef">
            <p> {{chef.name}} </p>
          </div>
            <a href="chefs/{{chef.id}}" class="link-chef">Visualizar</a>
          </div>
        </div>
      {%endfor%}
    </div>
  </section>
{% endblock content %}

